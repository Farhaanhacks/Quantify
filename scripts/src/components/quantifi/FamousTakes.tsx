// DEPRECATED — Famous Takes was removed from the product. This file is kept
// only as a compiling no-op so that extracting a project zip over an older
// checkout overwrites the old, broken version (which imported deleted demo
// data) rather than leaving it behind. Not imported anywhere. Safe to delete.
export default function FamousTakes() {
  return null;
}
