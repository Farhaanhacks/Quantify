// DEPRECATED — superseded by StockExplorer (the live /stock-analysis page).
// This file used to render a hardcoded demo stock and is no longer imported
// anywhere. It is kept only as a compiling no-op so that extracting a project
// zip over an older checkout overwrites the old, broken version instead of
// leaving a file that imports now-deleted demo data. Safe to delete.
export default function StockAnalysis() {
  return null;
}
