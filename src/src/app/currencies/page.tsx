import CurrencyTools from "@/components/quantifi/CurrencyTools";

export const metadata = {
  title: "Currencies · Quantifi",
  description: "Live FX converter and a multi-currency rates board.",
};

export default function CurrenciesPage() {
  return <CurrencyTools />;
}
