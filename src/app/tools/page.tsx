import ToolsSuite from "@/components/quantifi/ToolsSuite";

export const metadata = {
  title: "Tools · Quantifi",
  description: "Compare stocks and ETFs, calculate CAGR and dividends, and look up any symbol.",
};

export default function ToolsPage() {
  return <ToolsSuite />;
}
