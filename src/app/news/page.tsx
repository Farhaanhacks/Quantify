import NewsImpact from "@/components/quantifi/NewsImpact";

export const metadata = {
  title: "News Impact · Quantifi",
  description: "Market news mapped to the stocks, peers, sectors and ETFs it affects.",
};

export default function NewsPage() {
  return <NewsImpact />;
}
