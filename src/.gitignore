# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# package-lock.json IS tracked. Without it the deploy resolves every
# caret-ranged dependency fresh on each build, so the same commit can compile
# one day and fail the next, and a build that passes locally proves nothing
# about the one that runs on the server.
quantifi-latest.zip
