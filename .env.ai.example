# DeepSeek API key. Server-only: never prefix this with NEXT_PUBLIC_.
AI_QUANTIFI=your_deepseek_api_key

# Optional. Quantifi defaults to DeepSeek's fast current-generation model.
AI_QUANTIFI_MODEL=deepseek-v4-flash
